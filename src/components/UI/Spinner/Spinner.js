import React from "react";
import styles from "./Spinner.module.css";
import BeatLoader from "react-spinners/BeatLoader";
const Spinner = (props) => {
  return <BeatLoader color="#000" />;
};

export default Spinner;
